# Auto-detected camera configuration
# Generated by hardware auto-detection
# Review and copy desired sections to config.toml

[camera1]
    description = "Integrated_Webcam_HD: Integrate (mjpeg, 1280x720)"
    enabled = true
    camera = '/dev/video4'
    format = 'v4l2'
    size = "1280x720"
    fps = 30

    inputParameters = '-input_format mjpeg -rtbufsize 512M -thread_queue_size 4096'
    outputParameters = '-vcodec copy -an'
    recode = true

[camera2]
    description = "UVC Camera (h264, 1920x1080)"
    enabled = true
    camera = '/dev/video2'
    format = 'v4l2'
    size = "1920x1080"
    fps = 60

    inputParameters = '-rtbufsize 512M -thread_queue_size 4096'
    outputParameters = '-c:v copy -an'
    recode = false

[camera3]
    description = "UHA-UTCA: UHA-UTCA (mjpeg, 1920x1080)"
    enabled = true
    camera = '/dev/video0'
    format = 'v4l2'
    size = "1920x1080"
    fps = 60

    inputParameters = '-input_format mjpeg -rtbufsize 512M -thread_queue_size 4096'
    outputParameters = '-vcodec copy -an'
    recode = true

# =======================================================
# Detected H.264 encoders on this system
# =======================================================
# h264_vaapi - VAAPI (AMD/Intel on Linux)
# libx264 - Software encoder (always available)
