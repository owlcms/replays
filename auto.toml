# Auto-detected camera configuration
# Generated by hardware auto-detection
# Review and copy desired sections to config.toml

[camera1]
    description = "OV01A (yuyv422, 1280x720)"
    enabled = true
    camera = 'video=OV01A'
    format = 'dshow'
    size = "1280x720"
    fps = 30

    inputParameters = '-pixel_format yuyv422 -rtbufsize 512M -thread_queue_size 4096'
    outputParameters = '-c:v h264_qsv -preset medium -look_ahead 0 -rc_mode CBR -b:v 8M -g 30 -keyint_min 30 -r 30 -vsync cfr -an'
    recode = false

[camera2]
    description = "OBS Virtual Camera (nv12, 1920x1080)"
    enabled = true
    camera = 'video=OBS Virtual Camera'
    format = 'dshow'
    size = "1920x1080"
    fps = 60

    inputParameters = '-pixel_format nv12 -rtbufsize 512M -thread_queue_size 4096'
    outputParameters = '-c:v h264_qsv -preset medium -look_ahead 0 -rc_mode CBR -b:v 8M -g 60 -keyint_min 60 -r 60 -vsync cfr -an'
    recode = false

# =======================================================
# Detected H.264 encoders on this system
# =======================================================
# h264_qsv - Intel GPU (QSV)
# libx264 - Software encoder (always available)
