# HTTP server port
port = 8091

# Directory to store video files
videoDir = 'videos'

# Video resolution
width = 1280
height = 720

# Frames per second
fps = 30

# FFmpeg configuration - Linux defaults
#ffmpegPath = '/usr/bin/ffmpeg'
#ffmpegCamera = '/dev/video0'
#ffmpegFormat = 'v4l2'

# FFmpeg configuration - Windows defaults
# To find the camera name use the Windows command
#   ffmpeg -f dshow -list_devices true -i dummy
#ffmpegCamera = 'video=OV01A'
#ffmpegPath = 'C:\ProgramData\chocolatey\bin\ffmpeg.exe'
#ffmpegFormat = 'dshow'

# FFmpeg configuration - WSL defaults
# To find the camera name use the Windows command. The video= prefix is required.
#   ffmpeg -f dshow -list_devices true -i dummy
#ffmpegCamera = 'video=OV01A'
#ffmpegPath = '/mnt/c/ProgramData/chocolatey/bin/ffmpeg.exe'
#ffmpegFormat = 'dshow'
